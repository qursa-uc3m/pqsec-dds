# Protocol documents

- [PQSec DDS authentication protocol 1.0](pqsec-auth-v1.md) defines the
  experimental protocol currently implemented by the OpenDDS adapter.

The existing CycloneDDS adapter has not yet been changed to this wire format.
Protocol alignment and compatibility versioning will happen before adding
interoperability tests.
