# Interoperability tests

Cross-DDS interoperability is reserved for a future feature.  These tests will
eventually run CycloneDDS and OpenDDS participants against each other after the
two adapters share the same protocol identifier, token fields, transcript
serialization, algorithm identifiers, challenge rules, and secret handling.
