# Protocol vectors

This directory will contain implementation-independent inputs and expected
serialized transcripts, credential hashes, signatures, KEM material, and
shared-secret outputs.

Vectors will be added when the CycloneDDS adapter is aligned with the shared
protocol.  Keeping them independent of either DDS implementation will allow
both adapters to consume the same conformance data.
